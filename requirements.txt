# Core dependencies (use numpy<2.0 for FAISS compatibility)
numpy>=1.20.0,<2.0
scipy>=1.7.0,<1.15
scikit-learn>=1.0.0,<1.7
pandas>=2.0.0,<3.0.0
matplotlib>=3.0.0,<4.0.0

# Optional: for parallel processing
# joblib>=1.0.0

# FAISS for fast KNN (CPU version)
faiss-cpu>=1.7.0

# Optional: for fast KNN with FAISS (GPU version, requires CUDA)
# faiss-gpu>=1.7.0

# Environment setup:
# conda create -n clustering python=3.10
# conda activate clustering
# pip install -r requirements.txt
